# Ranges of Strings

my_range = range(1,21)
my_list = ([str(i) for i in my_range])
print(my_list)

#print(list(map(str, my_range))) # an iterable and a function is passed in map and that function is performed on every element
# returns an object